{
  "name": "chameleon-chess-logic",
  "version": "1.0.0",
  "description": "The game logic engine for the board game Chameleon Chess",
  "files": [
    "build"
  ],
  "main": "build/main.js",
  "scripts": {
    "prepare": "npm run ts",
    "start": "npm run ts && node test",
    "test": "npm run ts && mocha ./test/**/*.js",
    "ts": "rm -rf build && tsc",
    "ts:debug": "rm -rf build && tsc --sourceMap"
  },
  "author": "Hannes Dröse",
  "license": "UNLICENSED",
  "devDependencies": {
    "mocha": "^7.0.0",
    "typescript": "^3.7.4"
  }
}
